// Returns the list of available festival edition slugs.
// Mockup implementation — replace with config or endpoint later.

export async function getSlugs(): Promise<string[]> {
  return ['ba2024', 'ba2025', 'ba2026'];
}
